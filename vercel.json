{
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "pnpm run build",
        "installCommand": "pnpm install"
      }
    }
  ],
  "build": {
    "env": {
      "NEXT_SHARP_PATH": "/tmp/node_modules/sharp"
    }
  }
} 